requests
gTTS
python-dotenv
Pillow

# New dependencies for advanced features
anthropic>=0.18.0

# High-quality video rendering (optional but recommended)
moviepy>=1.0.3

# For accurate subtitle timing (100% FREE)
openai-whisper>=20231117
# Note: Whisper also requires ffmpeg (already used for video processing)
# Optional: numba for faster processing (automatically used if available)

# Alternative paid option: ElevenLabs API (requires API key)

# YouTube Data API v3 integration
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.100.0

# Local TTS (Phase 2)
torch<2.6.0
TTS>=0.22.0
mecab-python3>=1.0.9
unidic-lite>=1.0.8

# RSS Feed Parsing (Phase 4)
feedparser>=6.0.10
